const ExportObject = require("./helpers");

const now = Date.now();

const { num } = require("./helpers");

console.log(ExportObject);

console.log(ExportObject.CreateNormalDate(now));

console.log(num);